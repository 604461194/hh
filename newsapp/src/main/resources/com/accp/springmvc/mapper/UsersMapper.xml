<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "mybatis-3-mapper.dtd" "mybatis-3-mapper.dtd" >
<mapper namespace="com.accp.springmvc.mapper.UsersMapper">
	<select id="select" resultType="Users">
		SELECT * FROM `users` WHERE `userName`=#{userName}
	</select>
	<insert id="insert">
		INSERT INTO `news`.`users` (`userName`)
		VALUES
		(#{userName}) ;

	</insert>
</mapper>
